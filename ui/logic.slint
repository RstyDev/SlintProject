import {VentaFND} from "structs.slint";

export global Logic{
    pure callback pagar(int,int)->int;
    pure callback test(string);
    pure callback get_venta_actual(bool)->VentaFND;
    pure callback open_login();
    pure callback try_login(string,string)->string;
    
    in-out property <VentaFND> venta;
}